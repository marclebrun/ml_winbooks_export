<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <data>

        <record
            id="action_account_winbooks_export"
            model="ir.actions.act_window">

            <field name="name">Winbooks Export (ML)</field>
            <field name="res_model">ml.winbooks.export</field>
            <field name="type">ir.actions.act_window</field>
            <field name="view_mode">tree,form</field>
            <field name="view_id" ref="ml_winbooks_export_wizard"/>
            <field name="context">{}</field>
            <field name="target">new</field>

        </record>

    </data>
</odoo>
